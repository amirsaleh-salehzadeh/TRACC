# Server port
server.port=8090

# Database configuration (using H2 in-memory database as an example)
spring.datasource.url=jdbc:h2:mem:ccinetdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate (JPA) configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
# Logging
logging.level.org.springframework=INFO
logging.level.com.yourapp=DEBUG

# Security settings (if needed)
# security.basic.enabled=true
# security.user.name=user
# security.user.password=password

# Customize other properties as per your requirements

